version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.12"

sphinx:
  configuration: docs/source/conf.py

formats:
  - pdf

python:
  install:
    - method: pip
      path: .
      exrra_requirements:
        - devel
